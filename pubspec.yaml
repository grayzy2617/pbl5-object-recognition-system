name: flutter_application_1
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  csv: ^8.0.0
  path_provider: ^2.1.5
  audioplayers: ^6.6.0
  intl: ^0.20.2
  image_picker: ^1.2.1
  flutter_tts: ^4.2.5
  speech_to_text: ^7.3.0
  permission_handler: ^12.0.1
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.2
  animated_text_kit: ^4.3.0
  confetti: ^0.8.0
  flutter_local_notifications: ^21.0.0
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0
  flutter_mjpeg: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/

# ==========================================
# 🔴 ĐÂY LÀ ĐOẠN ÉP PHIÊN BẢN ĐỂ SỬA LỖI
# ==========================================
dependency_overrides:
  http: ^1.6.0